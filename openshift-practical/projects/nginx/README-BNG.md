# 📁 OpenShift-Practical: Nginx এপ্লিকেশন ডেপ্লয়মেন্ট গাইড (OpenShift CRC)

> **বাস্তব ব্যবহারের জন্য প্রয়োজনীয় ধাপে ধাপে গাইড**  
> লেখক: Sumon  
> প্ল্যাটফর্ম: OpenShift CodeReady Containers (CRC)  
> লক্ষ্য: নন-রুট (unprivileged), সিকিউর, এবং এজ-টারমিনেটেড TLS সহ Nginx ওয়েব সার্ভার ডেপ্লয় করা  

---

## 📌 এই ফোল্ডারের উদ্দেশ্য কী?

এই ফোল্ডারটি OpenShift CRC-এ `nginxinc/nginx-unprivileged` ইমেজ ব্যবহার করে একটি **সিকিউর, নন-রুট Nginx ওয়েব সার্ভার** ডেপ্লয় করার জন্য তৈরি করা হয়েছে। এটি নিম্নলিখিত বৈশিষ্ট্য সমর্থন করে:

- ✅ নন-রুট ইউজারে কন্টেইনার রান (security best practice)
- ✅ `allowPrivilegeEscalation: false` এবং `drop: ["ALL"]` ক্যাপাবিলিটি ড্রপ
- ✅ Seccomp সিকিউরিটি প্রোফাইল
- ✅ OpenShift Route দিয়ে এজ-TLS টারমিনেশন
- ✅ `demo-app` নামক আলাদা Namespace তৈরি করে আইসোলেশন

---

## 🔧 প্রয়োজনীয় প্রাথমিক শর্তাবলী

1. আপনার কম্পিউটারে **OpenShift CRC** ইনস্টল এবং রান হচ্ছে।  
   → `crc status` কমান্ড দিয়ে চেক করুন।

2. আপনি OpenShift ক্লাস্টারে **kubeadmin বা অনুমোদিত ইউজার** হিসেবে `oc login` করেছেন।

3. `oc` CLI টুল ইনস্টল করা আছে (সাধারণত CRC ইনস্টলের সময় অটো আসে)।

---

## ❓ `oc` মানে কী? `kubectl` এর সাথে পার্থক্য কী?

- **`oc`** হলো **OpenShift Client** — এটি Kubernetes-এর `kubectl` এর একটি **সুপারসেট**।  
- `oc` শুধুমাত্র পড, ডেপ্লয়মেন্ট ম্যানেজ করে না, বরং OpenShift-এর নিজস্ব রিসোর্স যেমন: **Route, Project (Namespace), BuildConfig, DeploymentConfig, SCC**, ইত্যাদি ম্যানেজ করতে পারে।
- `kubectl` কেবল স্ট্যান্ডার্ড Kubernetes রিসোর্স ম্যানেজ করে।
- অর্থাৎ:  
  → **OpenShift = Kubernetes + Extra Features**  
  → **`oc` = `kubectl` + OpenShift-specific কমান্ড**

উদাহরণ:
```bash
oc get route          # OpenShift-specific
kubectl get ingress   # Kubernetes-এর সমতুল্য (কিন্তু OpenShift-এ Route ব্যবহার হয়)
```

---

## 🚀 ধাপে ধাপে ডেপ্লয়মেন্ট গাইড

### 1. গিটহাব রিপোজিটরি ক্লোন করুন

```bash
git clone https://github.com/your-username/openshift-practical.git
cd openshift-practical/projects/nginx
```

> ✅ Replace `your-username` with the actual GitHub username.

---

### 2. Namespace তৈরি করুন

```bash
oc apply -f ns-demo-app.yaml
```

**আউটপুট:**
```
namespace/demo-app created
```

**ভেরিফাই:**
```bash
oc get ns demo-app
```

---

### 3. Deployment + Service + Route একসাথে অ্যাপ্লাই করুন

```bash
oc apply -f nginx-deploy.yaml
```

এই ফাইলে তিনটি রিসোর্স আছে:
- Deployment (`nginx-deploy`)
- Service (`nginx-svc`)
- Route (`nginx-route`)

> 📌 লক্ষ্য করুন: `route.yaml` ফাইলটি **অপ্রয়োজনীয়**, কারণ `nginx-deploy.yaml`-এ একই Route আবার ডিফাইন করা হয়েছে।  
> একই নামে দুইটি Route থাকলে কনফ্লিক্ট হতে পারে। তাই আমরা **শুধু `nginx-deploy.yaml` ব্যবহার করব**।

---

### 4. ডেপ্লয়মেন্ট ও পড চেক করুন

```bash
oc get pods -n demo-app
```

**সফল আউটপুট (Running অবস্থায়):**
```
NAME                           READY   STATUS    RESTARTS   AGE
nginx-deploy-7d8f5b9c8-xk2lq   1/1     Running   0          30s
```

যদি **CrashLoopBackOff** দেখায়, তাহলে লগ চেক করুন:
```bash
oc logs -l app=nginx -n demo-app --tail=20
```

---

### 5. Route URL দেখুন এবং ওয়েবে অ্যাক্সেস করুন

```bash
oc get route nginx-route -n demo-app
```

**আউটপুট:**
```
NAME          HOST/PORT                            PATH   SERVICES     PORT   TERMINATION   INSECURE POLICY
nginx-route   nginx-route-demo-app.apps-crc.testing        nginx-svc   8080   edge          Redirect
```

ওয়েব ব্রাউজারে যান:  
👉 `https://nginx-route-demo-app.apps-crc.testing`

> 🔒 TLS এজ-টারমিনেশনের কারণে `http://` অটো রিডাইরেক্ট হবে `https://`-এ।

---

## 🛠️ অপারেশন এবং ম্যানেজমেন্ট

### আপডেট (যদি কোনো পরিবর্তন করেন)

```bash
oc apply -f nginx-deploy.yaml -n demo-app
```

### স্কেল করুন (রেপ্লিকা বাড়ান/কমান)

```bash
oc scale deployment/nginx-deploy --replicas=3 -n demo-app
```

### লগ মনিটর করুন

```bash
oc logs -f -l app=nginx -n demo-app
```

---

## 🗑️ সম্পূর্ণ ডিলিট করুন

সবকিছু একসাথে মুছে ফেলতে:

```bash
oc delete -f nginx-deploy.yaml -n demo-app
oc delete -f ns-demo-app.yaml
```

অথবা সরাসরি Namespace ডিলিট করলে সব রিসোর্স অটো মুছে যাবে:

```bash
oc delete namespace demo-app
```

---

## 📈 গুরুত্বপূর্ণ উন্নতি ও বেস্ট প্র্যাকটিস

### ✅ প্রস্তাবিত ফাইল যোগ করুন

1. **`kustomization.yaml`** – Kustomize ব্যবহার করে কনফিগ ম্যানেজ করার জন্য  
2. **`README-local.md`** – লোকাল টেস্টিং নোটস  
3. **`nginx-configmap.yaml`** – কাস্টম Nginx কনফিগারেশন (যেমন: custom server block)  
4. **`resource-limits.yaml`** – CPU/Memory লিমিট যোগ করুন (প্রোডাকশনের জন্য অপরিহার্য)

উদাহরণ: `resource-limits.yaml`

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy
  namespace: demo-app
spec:
  template:
    spec:
      containers:
      - name: nginx
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
```

> 📌 **সিকিউরিটি টিপ**: প্রোডাকশনে `:latest` ট্যাগ এড়িয়ে চলুন। স্পেসিফিক ভার্সন ব্যবহার করুন, যেমন: `nginxinc/nginx-unprivileged:1.25-alpine`

---

## 🧭 ভবিষ্যতের পরিকল্পনা (Roadmap)

| ফিচার | বর্ণনা |
|--------|--------|
| ✅ **Custom Nginx Config** | ConfigMap থেকে কনফিগ লোড |
| ✅ **Health Check (liveness/readiness)** | অটো রিস্টার্ট ও ট্রাফিক রাউটিং উন্নতি |
| ✅ **External PersistentVolume** | লগ বা কাস্টম ফাইল স্টোরেজ |
| ✅ **Monitoring (Prometheus + Grafana)** | মেট্রিক্স কালেকশন |
| ✅ **CI/CD ইন্টিগ্রেশন** | GitHub Actions দিয়ে অটো ডেপ্লয় |
| ✅ **Policy Enforcement** | OPA/kyverno দিয়ে সিকিউরিটি পলিসি |

---

## 📬 সাহায্য বা ফিডব্যাক?

যদি কোনো সমস্যা হয়, নিচের কমান্ডগুলি দিয়ে তথ্য সংগ্রহ করুন:

```bash
oc describe pod -l app=nginx -n demo-app
oc get events -n demo-app
oc get route nginx-route -n demo-app -o yaml
```

---

## 🌟 সারসংক্ষেপ

- এই প্রজেক্ট **OpenShift-এ নন-রুট, সিকিউর Nginx** ডেপ্লয়মেন্টের রেফারেন্স আর্কিটেকচার।
- `oc` CLI ব্যবহার করে CRC-এ সহজে ডেপ্লয়, মনিটর, আপডেট, ডিলিট করা যায়।
- প্রোডাকশনে যাওয়ার আগে **ট্যাগ ফিক্স, রিসোর্স লিমিট, হেলথ চেক** অবশ্যই যোগ করুন।

---

> ✨ **Happy DevOps-ing on OpenShift!**  
> — Sumon

--- 